$(document).ready(function(){
    // $("h2").siblings().css({"color": "aqua", "border": "2px solid red"});

    // $("h2").next().css({"color": "aqua", "border": "2px solid red"});

    // $("h2").next().css({"color": "red", "border": "2px solid red"});

    // $("h2").nextAll().css({"color": "red", "border": "2px solid red"});

    // $("h2").nextUntil('h6').css({"color": "red", "border": "2px solid red"});

    // $("h2").prev().css({"color": "aqua", "border": "2px solid red"});

    // $("h2").prevAll().css({"color": "aqua", "border": "2px solid red"});

   

  });